{
  "db": {
    "host": "DB_HOST",
    "port": "DB_PORT",
    "user": "DB_USER",
    "password": "DB_PASSWORD",
    "name": "DB_NAME",
    "schema": "DB_SCHEMA"
  },
  "sequelize": {
    "logging": "SQ_LOGGING",
    "pool": {
      "max": "POOL_MAX",
      "min": "POOL_MIN",
      "idle": "POOL_IDLE",
      "acquire": "POOL_ACQUIRE"
    }
  }
}
